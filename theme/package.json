{
  "name": "pf2018-theme",
  "version": "1.0.0",
  "description": "A theme for the Grommet React library",
  "main": "node_modules/grommet/src/js/index.js",
  "scripts": {
    "version": "npm run build && git add -A dist",
    "postversion": "git push && git push --tags",
    "build": "npm-sass ./src/grommet.scss > ./dist/grommet.css"
  },
  "author": "Processing.org",
  "license": "Apache-2.0",
  "dependencies": {
    "grommet": "^1.9.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "npm-sass": "^2.2.1"
  }
}
